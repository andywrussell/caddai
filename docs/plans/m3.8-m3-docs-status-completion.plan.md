# M3.8 — M3 documentation and milestone completion

GitHub issue: [#32](https://github.com/andywrussell/caddai/issues/32)
(sub-issue of the M3 parent, [#9](https://github.com/andywrussell/caddai/issues/9))

## Goal

Verify M3 (`player`/`statistics` subsystems) is genuinely complete on `main`,
reconcile documentation with the code that actually exists, and leave a
clean, accurate boundary for M4. Documentation-only — no new product
functionality, no simulation/learning/persistence/strategy changes, no ADR.

## Audit findings (pre-work)

- All M3 sub-issues except this one are **closed** and merged into `main`:
  #26 (M3.1, PR #35), #27 (M3.2, PR #36), #28 (M3.3, PR #37), #29 (M3.4,
  PR #39), #30 (M3.5, PR #40), #31 (M3.7, PR #41), #38 (M3.x finite
  statistics validation, PR #42), #43 (finite `ShotRecord` validation,
  PR #44).
- Confirmed in code: `CarryDistribution` (mean/stddev, finite-validated),
  `DirectionalDispersion` (lateral stddev/signed bias, finite-validated,
  fixed sign convention independent of handedness), `Club` (free-text
  name, `ClubCategory` taxonomy, `carry_distribution`, `dispersion`,
  `expected_carry_metres` computed field, `with_expected_carry()`),
  `Player` (`clubs`, `shot_history`), `ShotRecord` (`club_name` snapshot,
  `achieved_carry_metres`, `lateral_offset_metres`, optional `notes`,
  finite-validated).
- Confirmed `recommend_club()` ([src/caddai/strategy/recommend.py](../../src/caddai/strategy/recommend.py))
  selects purely on `club.expected_carry_metres` — `dispersion`/`category`
  are never read by it. `demo.py` prints them under an explicit
  "informational only — not used in club selection" banner. M1
  recommendation behaviour is otherwise unchanged.
- Project metadata audit (CaddAI Development, project #2): #26–#31, #38,
  #43 all `Status=Done`, `Milestone=M3`. #27 and #29's "linked pull
  requests" field is empty in the project view (their PRs — #36, #39 —
  referenced the issue number in the PR title, not a "Closes #" keyword,
  so GitHub didn't auto-link/auto-close) but both issues are correctly
  `state=CLOSED` and the code is verifiably on `main`. No metadata repair
  needed — issue state already matches reality; retroactively editing
  already-merged PR bodies is unnecessary churn, not a genuine
  inconsistency.
- Parent issue #9 remains open (correct — it closes when M3.8/#32 closes).

## Architect input

CaddAI Architect reviewed (read-only): **no ADR required** — pure
documentation correction, no dependency/API/unit/ownership/dependency-
direction change. Confirmed the proposed boundary statements are
code-accurate. Confirmed "M3 (complete)" is justified once #32 lands.
Flagged one addition: `ShotRecord.club_name` has no cross-validation
against `Player.clubs` — name this explicitly alongside the finite-value
validation note, and add a corresponding backlog item (genuine, not
speculative — a foreseeable failure mode if a club is renamed/replaced).
Also flagged `docs/domain-model.md` line 3 ("No domain types are
implemented yet") as stale and now inconsistent with the rest of this
pass — small enough to fold in here rather than leave contradicting
`architecture.md`/`roadmap.md` in the same PR.

## Tasks

### Task 1 — Documentation reconciliation

- Owning engineer: Player Engineer
- Files: `docs/player-model.md`, `docs/roadmap.md`, `docs/architecture.md`,
  `docs/backlog.md`, `docs/domain-model.md`
- Acceptance criteria:
  - `docs/player-model.md`: update the status note to list all M3
    capabilities as actually implemented (per the audit above, including
    finite-value validation from #38/#43), explicitly state the `club_name`
    non-cross-validation fact, and add explicit M3 boundary statements:
    manually-supplied parameters only (no fitting/learning from
    `shot_history`), no Monte Carlo simulation, no use of
    dispersion/category in strategy decisions, no persistence/storage
    technology selected, no mobile/cloud runtime decision made.
  - `docs/roadmap.md`: mark the M3 entry `*(complete)*` (matching
    M1/M2's style) and note issues #38/#43 as part of M3's finite-value
    hardening (currently unmentioned).
  - `docs/architecture.md`: correct the stale top status banner — `player`/
    `statistics` are now substantially implemented per M3, not "a minimal
    M1 vertical slice"; `simulation`/`llm`/`api`/`cli` still don't exist.
  - `docs/backlog.md`: add one new candidate item for stable club-identity/
    history semantics if `ShotRecord.club_name` string snapshots become
    insufficient (e.g. club renamed/replaced), scoped as research/non-
    committed like existing items. Do not duplicate the existing
    fitting/learning deferral item — confirm it still reads correctly.
  - `docs/domain-model.md`: fix the stale line 3 ("No domain types are
    implemented yet") to reflect that `course`/`gps`/`player`/`statistics`
    now have concrete types, without duplicating per-subsystem detail
    that belongs in the owning docs.
  - No source code changes. No new ADR. No speculative/invented backlog
    items beyond the one genuine addition above.
  - `CHANGELOG.md` gets one `### Changed` entry for this documentation
    pass (added by the Integrator).

## Parallelism

Single docs-only task, one owning engineer (Player Engineer, per the M2.6
precedent of the closeout issue's owning engineer handling cross-cutting
doc updates) — no parallel delegation, no QA Engineer test-writing task
(no code changes to test).

## GitHub/project metadata

No repair needed beyond this issue's own lifecycle (close #32 via this
PR's "Closes #32", which also closes parent #9 only if GitHub is configured
to cascade — confirm manually since GitHub does not auto-close a parent
tracking issue just because all sub-issues are closed; #9 requires its own
explicit close or remains open by design as a milestone-tracking issue).

## Escalations

None.
