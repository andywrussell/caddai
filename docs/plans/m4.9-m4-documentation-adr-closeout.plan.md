# M4.9 — M4 documentation, ADR status, and milestone closeout

GitHub issue: #57 (parent milestone: #10 — M4).

## Goal

Audit M4 (`player`/`statistics`/`simulation`) implementation against ADR
0006/ADR 0007 and the M4.0 research spike's conclusions, bring
documentation up to date, record what remains provisional/deferred, and
close the milestone. Documentation/closeout only — no new production
functionality, no M5 implementation planning, no M5 issue tree, no roadmap
renumbering.

## Verification performed (read-only, before editing)

- Confirmed M4.0–M4.8 (issues #47, #49–#56) are all `CLOSED`; their PRs
  (#48, #58–#70) are all `MERGED`. No unresolved implementation dependency
  found for M4.9 to report as `NEEDS_DECISION`.
- Confirmed ADR 0006 and ADR 0007 are already `Accepted` (flipped during
  M4.2, issue #50) and their text matches implementation (bivariate
  Student-t family/parameterisation; `PopulationPrior` replaceability
  contract) — no drift found.
- Confirmed `docs/player-model.md` and `docs/strategy-engine.md` already
  carry detailed, accurate per-issue M4.1–M4.8 narratives (written
  incrementally during each issue's PR) — no stale Gaussian/flat-carry/
  final-distance-as-carry/record-level-quality/global-RNG wording found.
- Confirmed the M5 parent issue (#11) already records course-relative
  outcome mapping as an explicit M5 prerequisite (not a generic backlog
  item), satisfying that specific issue #57 acceptance criterion.
- Found `docs/roadmap.md` has a duplicated/stale status blockquote (two
  overlapping intro paragraphs, one omitting M3/M4) — genuine stale-doc
  defect to fix.
- Found `docs/backlog.md` retains several pre-M4.0 placeholder items now
  resolved by M4.0–M4.2 (e.g. "decide whether a `PlayerShotDistribution`
  abstraction is needed", "update tracking issue #10 title") — stale,
  remove. Confirmed M4.0's explicitly-deferred items (severe-miss mixture,
  lateral skew, lie-specific multipliers, learned/ML population prior,
  psychological-pressure rejection, `Wind`/`LieType` unification,
  handicap × club calibration data collection) were not yet in
  `docs/backlog.md` — add them.
- Confirmed no "post-M4 roadmap-review checkpoint" text exists yet on
  `main` (a related PR, #71, adds a similar note but is still open/
  unmerged) — add a concise checkpoint note to `docs/roadmap.md` as part
  of this closeout, since issue #57 requires it explicitly.

## Scope of edits

1. `docs/roadmap.md` — fix duplicated status header; mark M4 complete
   (matching M1–M3 convention); add a concise post-M4 roadmap-review
   checkpoint note (project pauses before detailed M5 planning; lists the
   concerns to jointly reassess; states the external agentic/multi-repo
   research report is research input only, not accepted architecture).
2. `docs/architecture.md` — update the status banner to reflect M4
   complete; add a concise "M4 forward shot-production pipeline" section
   (population prior → onboarding → baseline → history → partial-pooling
   update → current distribution → seeded sampling → intrinsic outcome →
   optional environment transform → environment-adjusted outcome),
   explicit that this is a forward pipeline (no course-relative
   outcome/final resting position yet), linking to ADR 0006/0007 and
   `player-model.md`/`strategy-engine.md` rather than duplicating detail.
3. `docs/backlog.md` — remove stale resolved pre-M4.0 items; add newly
   identified deferred-from-M4 items.
4. `docs/player-model.md` — status header: mark M4 complete (Player
   Engineer edit).
5. `docs/strategy-engine.md` — status header: mark M4 `simulation` scope
   complete, course-relative mapping still M5+ (Strategy Engineer edit).
6. `CHANGELOG.md` — add a concise M4.9 closeout entry under `[Unreleased]`.
7. GitHub: add a completion-summary comment to parent issue #10 (do not
   close it — precedent from M3's closeout, issue #45/#32, is that the
   closeout PR closes only its own issue, not the parent milestone issue,
   which issue #9 shows remains open). Draft PR body includes `Closes #57`
   only.

## Non-goals

No M5 implementation planning or issue tree, no roadmap renumbering, no
new dependency, no new ADR, no production code changes (unless a genuine
closeout defect is found and explicitly reported), no telemetry, no
synthetic-validation-harness implementation, no repository-split/DevOps/
agentic-orchestration decisions.

## Acceptance criteria

Per issue #57: docs describe only what is implemented; every provisional
numeric parameter remains traceable to its config/source location
(already true, unchanged); M4→M5 course-relative-mapping dependency
recorded unambiguously on issue #11 (already true); full quality gate
passes.
